export enum SignTargets {
  registration = 'registration',
  forgotPassword = 'forgotPassword',
}

export enum Roles {
  user = 'user',
  admin = 'admin',
  superhero = 'superhero',
}

export enum TokenTypes {
  access = 'access',
  refresh = 'refresh',
}
